#include <stdio.h>
#include <cs50.h>

int main(void)
{
    // print 10 hashes
    for (int i = 0; i <= 10; i++)
    {
        // print the number of i to double check
        printf("i is now %i\n", i);
        printf("#\n");
    }
}